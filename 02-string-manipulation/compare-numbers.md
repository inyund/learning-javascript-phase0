# ⚖️ Exercise 3: Bandingkan Angka

## Problem Statement
Diberikan dua variabel `angka1` dan `angka2`:
- Jika `angka2 > angka1` ➡️ cetak `true`
- Jika `angka1 > angka2` ➡️ cetak `false`
- Jika `angka1 === angka2` ➡️ cetak `"-1"`
