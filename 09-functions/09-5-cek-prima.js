function cekPrima(n) {
  // Kembalikan true jika n prima, false jika bukan
}

console.log(cekPrima(7));   // true
console.log(cekPrima(10));  // false
console.log(cekPrima(1));   // false
console.log(cekPrima(2));   // true
