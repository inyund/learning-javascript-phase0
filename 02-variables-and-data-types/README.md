# 📝 Bab 02: Variables & Data Types

## 🎯 Objective
1. Memahami deklarasi variabel (`let`, `const`).
2. Memahami perbedaan *primitive immutability* (String) vs *reference mutability* (Array).
3. Menggunakan operator `typeof` dan mengevaluasi kondisi nilai *falsy*.
